export enum Routes {
    EONET = '/eonet',
    EPIC = '/epic'
}

export enum EonetRoutes {
    CATEGORIES = '/categories',
    EVENTS = '/events'
}

export enum EpicRoutes {
    IMAGE_URL = '/image-url'
}